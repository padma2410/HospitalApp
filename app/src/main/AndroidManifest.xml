<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.padma.hp.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GalleryActivity" />
        <activity android:name=".FullActivity" />
        <activity android:name=".FacilityActivity" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".FindDoctorActivity" />
        <activity android:name=".DiabeticActivity" />
        <activity android:name=".RadiologyActivity" />
        <activity android:name=".LaboratoryActivity" />
        <activity android:name=".Op_theatreActivity" />
        <activity android:name=".Blood_bankActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".SplashActivity"></activity>
    </application>

</manifest>